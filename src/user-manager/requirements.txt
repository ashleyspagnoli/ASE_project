# --- DEPENDENZE CORE ---
fastapi
uvicorn[standard]       # Server ASGI con supporto standard

# --- DATABASE & UTILITY ---
pymongo==4.8.0          # Driver MongoDB
pydantic                # Validazione dati
python-dotenv           # Gestione variabili d'ambiente (.env)
dnspython               # Necessario per i server DNS di MongoDB
email-validator         # Validazione formale indirizzi email

# --- SICUREZZA & AUTENTICAZIONE ---
# SOSTITUITO: python-jose con pyjwt
pyjwt                   # Nuova libreria per gestire i token JWT

# PASSWORD HASHING
passlib[argon2]         # Base per l'hashing
argon2-cffi             # Implementazione C per Argon2

# --- FILE E CRYPTOGRAPHY (utilità) ---
python-multipart        # Parsing form-data (necessario per OAuth2PasswordRequestForm)
cryptography            # Mantienilo: serve per il tuo modulo 'crypto.py' (Fernet, ecc.)

# --- DEPENDENZE PER TEST ---
pytest
pytest-asyncio
httpx
pytest-mock
bleach